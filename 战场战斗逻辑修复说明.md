# 战场战斗逻辑修复说明

## 问题描述

1月28日猛虎战场军力人数为1vs1，但在1月29日00:00-06:00之间的战斗时间内并未发生战斗，未能决出猛虎战王。

## 问题原因

定时任务已经配置（每天00:05自动执行），但在创建 `BattlefieldService` 时缺少 `beast_pvp_service` 参数，导致战斗引擎无法正常工作。

## 修复方案

### 修改文件：`infrastructure/scheduler.py`

在 `_run_daily_battlefield()` 函数中添加 `BeastPvpService` 的创建：

```python
def _run_daily_battlefield():
    """每日自动开赛任务：依次跑猛虎、飞鹤战场"""
    logger.info("[Scheduler] 开始执行每日古战场自动开赛任务")

    player_repo = MySQLPlayerRepo()
    beast_repo = MySQLPlayerBeastRepo()
    battle_repo = MySQLBattlefieldBattleRepo()
    
    # 导入并创建 BeastPvpService
    from application.services.beast_pvp_service import BeastPvpService
    beast_pvp_service = BeastPvpService()

    service = BattlefieldService(
        player_repo=player_repo,
        player_beast_repo=beast_repo,
        battle_repo=battle_repo,
        beast_pvp_service=beast_pvp_service,  # 添加此参数
    )
```

## 战斗逻辑说明

### 1. 淘汰赛规则

战场采用单淘汰赛制，战斗规则与闯塔、镇妖、擂台相同：
- 使用统一的PVP战斗引擎
- 幻兽对战，回合制战斗
- 生成详细战报（包含每回合的攻击、伤害、剩余血量）

### 2. 匹配规则

#### 情况1：偶数人（如2vs2、4vs4）
```
第一轮：
  张三 vs 李四 → 张三胜
  王五 vs 钱六 → 王五胜

第二轮：
  张三 vs 王五 → 张三胜

结果：张三成为战王
```

#### 情况2：奇数人（如3vs2）
```
第一轮（5人）：
  随机轮空：王五（直接晋级）
  张三 vs 李四 → 张三胜
  钱六 vs 赵七 → 钱六胜

第二轮（3人）：
  随机轮空：钱六（直接晋级）
  张三 vs 王五 → 张三胜

第三轮（2人）：
  张三 vs 钱六 → 张三胜

结果：张三成为战王
```

### 3. 轮空机制

- 每轮开始前检查参赛人数
- 如果是奇数人，随机选择一人轮空
- 轮空者直接晋级下一轮，不消耗战斗次数
- 其他人正常配对战斗

### 4. 阵营分配

- 报名玩家随机打乱顺序
- 前一半分配到红队
- 后一半分配到蓝队
- 阵营仅用于显示，不影响战斗匹配

### 5. 战报生成

每场战斗生成完整战报，包含：
- 对战双方信息（玩家名、阵营）
- 轮次和场次编号
- 战斗详情（多战多回合）
- 胜负结果
- 战斗数据（battle_data）

## 定时任务配置

### 执行时间
- **每天 00:05** 自动执行
- 使用中国时区（UTC+8）

### 执行流程
1. 检查猛虎战场今日是否已有战报
   - 有：跳过
   - 无：执行战斗
2. 检查飞鹤战场今日是否已有战报
   - 有：跳过
   - 无：执行战斗
3. 记录执行结果到日志

### 防重复机制
通过检查 `battlefield_battle_log` 表中当天的记录，避免同一天重复执行。

## 战场类型配置

### 猛虎战场
- 等级范围：20-39级
- 战王称号：猛虎战王

### 飞鹤战场
- 等级范围：40-100级
- 战王称号：飞鹤战王

## 报名机制

### 报名时间
- 每天 06:00 - 20:00

### 报名条件
- 玩家等级在对应战场的等级范围内
- 同一玩家同一天只能报名一次

### 报名表
- 表名：`battlefield_signup`
- 字段：user_id, battlefield_type, signup_date, signup_time

### 清理机制
- 战斗结束后自动清理当日报名记录
- 为第二天的报名做准备

## 测试方法

### 方法1：手动触发（测试用）
```python
python test_battlefield_battle.py
```

### 方法2：通过API触发
```bash
POST /api/battlefield/run
{
  "type": "tiger"  # 或 "crane"
}
```

注意：需要设置环境变量 `BATTLEFIELD_ALLOW_TIME_BYPASS=1` 才能在非战斗时间触发。

### 方法3：等待定时任务
- 等到第二天 00:05
- 查看日志确认执行情况
- 查看战报验证结果

## 验证方法

### 1. 查看日志
```
[Scheduler] 开始执行每日古战场自动开赛任务
[Scheduler] tiger 战场开赛成功，期数=763, 参赛人数=5, 冠军=张三
[Scheduler] crane 战场开赛成功，期数=763, 参赛人数=8, 冠军=李四
[Scheduler] 每日古战场自动开赛任务完成
```

### 2. 查询战报
```sql
-- 查看最新期数
SELECT MAX(period) FROM battlefield_battle_log WHERE battlefield_type = 'tiger';

-- 查看战报列表
SELECT * FROM battlefield_battle_log 
WHERE battlefield_type = 'tiger' AND period = 763
ORDER BY round_num, match_num;
```

### 3. 前端验证
- 打开战场页面
- 查看"昨日战况"是否显示战报
- 查看"上期战绩"是否显示战王信息
- 查看"我的战绩"是否显示个人战绩

## 注意事项

1. **报名人数不足**：如果报名人数少于2人，不会开赛
2. **无幻兽玩家**：如果玩家没有出战幻兽，判为直接失败
3. **期数计算**：基于日期自动计算，同一天的期数相同
4. **时间窗口**：正式环境必须在00:00-06:00才能战斗
5. **日志记录**：所有执行情况都会记录到日志文件

## 相关文件

- `application/services/battlefield_service.py` - 战场服务（战斗逻辑）
- `infrastructure/scheduler.py` - 定时任务配置
- `infrastructure/db/battlefield_repo_mysql.py` - 战报数据访问
- `interfaces/routes/battlefield_routes.py` - 战场API接口
- `test_battlefield_battle.py` - 测试脚本
