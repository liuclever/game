# 连胜竞技场功能需求

## 📋 功能概述

点击"竞技"直接进入连胜竞技场界面

---

## ⏰ 开放时间

每天 8:00 - 23:00

---

## 🎮 核心玩法

### 匹配机制
- 每次显示 2 个切磋对手
- 系统每隔 5 分钟自动刷新对手
- 可花费 50 元宝手动刷新

### 匹配规则
- 同等级段位匹配（1-10级、11-20级、21-30级...）
- 没人时向下兼容
- 例如：76级 vs 79级（都在 71-80 级段位）

### 连胜机制
- 胜利一次 = 连胜 +1
- 失败一次 = 连胜归零
- 连胜次数可无限叠加

### 活力消耗
- 6 连胜之前：每次消耗 80 活力
- 6 连胜之后：每次消耗 15 活力

---

## 🎁 连胜奖励（每天只能领取一次）

| 连胜次数 | 奖励内容 |
|---------|---------|
| 1 连胜 | 铜钱 1,000 + 双倍卡 1 个 + 七类结晶随机 1 个 |
| 2 连胜 | 铜钱 5,000 + 强力捕捉球 1 个 + 七类结晶随机 1 个 |
| 3 连胜 | 铜钱 10,000 + 化仙丹 1 个 + 七类结晶随机 1 个 |
| 4 连胜 | 铜钱 50,000 + 活力草 1 个 + 七类结晶随机 1 个 |
| 5 连胜 | 铜钱 100,000 + 活力草 2 个 + 小喇叭 2 个 |
| 6 连胜 | 铜钱 150,000 + 重生丹 2 个 + 神·逆鳞碎片 1 个 |

---

## 👑 连胜大奖（全服第一）

**奖励**：铜钱 600,000 + 追魂法宝 1 个 + 金袋 5 个 + 招财神符 1 个

**条件**：全服当天连胜次数最多的玩家（每天只能领取一次）

---

## 📊 界面显示

### 1. 当前连胜王
实时显示当天连胜次数最高的玩家及其连胜次数

### 2. 今日连胜榜
显示全服今日连胜次数最高的 10 名玩家，实时更新

### 3. 历届连胜王
显示近 30 天的连胜王名字和连胜次数

### 4. 刷新倒计时
显示：`0分26秒后将自动刷新`

### 5. 对手信息
```
霜降 (76级). 切磋
我看见了生生不息的激荡 (79级). 切磋
```

### 6. 刷新按钮
```
刷新  立即刷新(消耗50元宝)
```

---

## 🗄️ 数据库设计

### 表：arena_streak（连胜竞技场记录）

```sql
CREATE TABLE arena_streak (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    current_streak INT DEFAULT 0 COMMENT '当前连胜次数',
    max_streak_today INT DEFAULT 0 COMMENT '今日最高连胜',
    total_battles_today INT DEFAULT 0 COMMENT '今日战斗次数',
    last_battle_time DATETIME COMMENT '最后战斗时间',
    last_refresh_time DATETIME COMMENT '最后刷新时间',
    claimed_rewards TEXT COMMENT '已领取的奖励(JSON)',
    claimed_grand_prize TINYINT(1) DEFAULT 0 COMMENT '是否领取大奖',
    record_date DATE COMMENT '记录日期',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_user_date (user_id, record_date),
    INDEX idx_max_streak (record_date, max_streak_today)
);
```

### 表：arena_streak_history（历届连胜王）

```sql
CREATE TABLE arena_streak_history (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    nickname VARCHAR(50) NOT NULL,
    max_streak INT NOT NULL COMMENT '最高连胜次数',
    record_date DATE NOT NULL COMMENT '记录日期',
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY uk_date (record_date),
    INDEX idx_date (record_date)
);
```

---

## 🔧 API 接口设计

### 1. GET /api/arena-streak/info
获取竞技场信息（当前连胜、对手列表、刷新倒计时等）

### 2. POST /api/arena-streak/refresh
刷新对手（消耗 50 元宝）

### 3. POST /api/arena-streak/battle
开始切磋战斗

### 4. GET /api/arena-streak/ranking
获取今日连胜榜（前 10 名）

### 5. GET /api/arena-streak/history
获取历届连胜王（近 30 天）

### 6. POST /api/arena-streak/claim-reward
领取连胜奖励

### 7. POST /api/arena-streak/claim-grand-prize
领取连胜大奖

---

## 📝 开发步骤

1. ✅ 创建数据库表
2. ✅ 创建后端 API 路由
3. ✅ 实现匹配逻辑
4. ✅ 实现战斗逻辑
5. ✅ 实现奖励发放
6. ✅ 创建前端页面
7. ✅ 测试功能

---

## 🎯 技术要点

- 等级段位匹配算法
- 自动刷新倒计时
- 连胜状态管理
- 每日重置机制
- 全服排行榜
- 实时更新显示
