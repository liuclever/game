# 召唤之王 - 动态功能实现

## 📋 问题描述

召唤之王挑战赛页面的"预选赛动态"部分显示的是写死的固定文字，不是真实的挑战记录。

## 🔧 解决方案

### 1. 创建挑战记录表

**文件**: `sql/014_king_challenge_logs.sql`

```sql
CREATE TABLE IF NOT EXISTS king_challenge_logs (
    id INT AUTO_INCREMENT PRIMARY KEY,
    challenger_id INT NOT NULL COMMENT '挑战者ID',
    challenger_name VARCHAR(50) NOT NULL COMMENT '挑战者昵称',
    defender_id INT NOT NULL COMMENT '防守者ID',
    defender_name VARCHAR(50) NOT NULL COMMENT '防守者昵称',
    challenger_wins BOOLEAN NOT NULL COMMENT '挑战者是否胜利',
    challenger_rank_before INT NOT NULL COMMENT '挑战者挑战前排名',
    challenger_rank_after INT NOT NULL COMMENT '挑战者挑战后排名',
    defender_rank_before INT NOT NULL COMMENT '防守者挑战前排名',
    defender_rank_after INT NOT NULL COMMENT '防守者挑战后排名',
    area_index INT NOT NULL COMMENT '赛区编号',
    challenge_time DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '挑战时间',
    INDEX idx_area_time (area_index, challenge_time),
    INDEX idx_challenger (challenger_id, challenge_time),
    INDEX idx_defender (defender_id, challenge_time)
);
```

### 2. 挑战时保存记录

**文件**: `interfaces/routes/king_routes.py`

在挑战接口中添加保存记录的代码：

```python
# 保存挑战记录（用于显示动态）
cursor.execute(
    """INSERT INTO king_challenge_logs 
       (challenger_id, challenger_name, defender_id, defender_name, 
        challenger_wins, challenger_rank_before, challenger_rank_after,
        defender_rank_before, defender_rank_after, area_index)
       VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s)""",
    (user_id, attacker.nickname or f"玩家{user_id}", 
     target_user_id, defender.nickname or f"玩家{target_user_id}",
     challenger_wins, my_rank, new_rank, 
     target_rank, my_rank if challenger_wins else target_rank,
     my_rank_info['area_index'])
)
```

### 3. 添加获取动态API

**文件**: `interfaces/routes/king_routes.py`

```python
@king_bp.get("/dynamics")
def get_king_dynamics():
    """获取预选赛动态（最近的挑战记录）"""
    user_id = get_current_user_id()
    if not user_id:
        return jsonify({"ok": False, "error": "请先登录"})
    
    # 获取玩家的赛区
    rank_info = ensure_king_rank(user_id)
    area_index = rank_info['area_index']
    
    # 获取该赛区最近20条挑战记录
    logs = execute_query(
        """SELECT 
            challenger_id, challenger_name, defender_id, defender_name,
            challenger_wins, challenger_rank_before, challenger_rank_after,
            defender_rank_before, defender_rank_after, challenge_time
           FROM king_challenge_logs
           WHERE area_index = %s
           ORDER BY challenge_time DESC
           LIMIT 20""",
        (area_index,)
    )
    
    # 格式化动态文本
    dynamics = []
    for log in logs:
        time_str = log['challenge_time'].strftime('%m-%d %H:%M')
        
        if log['challenger_wins']:
            # 挑战者胜利
            if log['challenger_rank_after'] < log['challenger_rank_before']:
                text = f"{log['challenger_name']} 挑战 {log['defender_name']}，大胜而归，排名从第{log['challenger_rank_before']}名上升至第{log['challenger_rank_after']}名"
            else:
                text = f"{log['challenger_name']} 挑战 {log['defender_name']}，大胜而归，排名保持第{log['challenger_rank_after']}名"
        else:
            # 防守者胜利
            text = f"{log['challenger_name']} 挑战 {log['defender_name']}，惜败，排名保持第{log['challenger_rank_after']}名"
        
        # 判断是否与当前玩家相关
        is_related = (log['challenger_id'] == user_id or log['defender_id'] == user_id)
        
        dynamics.append({
            "time": time_str,
            "text": text,
            "canView": is_related,
            "challengerId": log['challenger_id'],
            "defenderId": log['defender_id']
        })
    
    return jsonify({
        "ok": True,
        "dynamics": dynamics
    })
```

### 4. 前端调用API

**文件**: `interfaces/client/src/features/king/SummonKingChallengePage.vue`

**修改前**（写死的数据）：
```javascript
const dynamics = ref([
  { time: '12-04 22:59', text: '洋柿子 挑战你，你大胜而归，排名保持不变', view: true },
  { time: '12-04 14:12', text: '你挑战 功夫熊猫，你惜败，排名保持不变', view: true },
  { time: '12-03 05:43', text: '荒天帝 挑战你，你大胜而归，排名保持不变', view: true },
])
```

**修改后**（从API获取）：
```javascript
const dynamics = ref([])

const loadDynamics = async () => {
  try {
    const res = await http.get('/king/dynamics')
    if (res.data.ok) {
      dynamics.value = res.data.dynamics || []
    }
  } catch (e) {
    console.error('加载动态失败', e)
  }
}

onMounted(() => {
  loadKingInfo()
  loadRewardInfo()
  loadDynamics()  // 加载动态
})
```

**模板修改**：
```html
<div class="section title2">【预选赛动态】<a class="link gray"> 更多</a></div>
<div v-if="dynamics.length === 0" class="section gray">暂无动态</div>
<div v-for="(d, idx) in dynamics" :key="idx" class="section">
  <span class="gray">{{ d.time }}</span> {{ d.text }}
  <a v-if="d.canView" class="link"> 查看</a>
</div>
```

## 🎮 显示效果

### 有动态时
```
【预选赛动态】 更多
01-10 14:30 张三 挑战 李四，大胜而归，排名从第5名上升至第3名 查看
01-10 14:25 王五 挑战 赵六，惜败，排名保持第8名
01-10 14:20 孙七 挑战 周八，大胜而归，排名保持第2名
```

### 没有动态时
```
【预选赛动态】 更多
暂无动态
```

## 📡 API说明

### GET `/api/king/dynamics`

**说明**：获取玩家所在赛区的最近20条挑战记录

**返回格式**：
```json
{
  "ok": true,
  "dynamics": [
    {
      "time": "01-10 14:30",
      "text": "张三 挑战 李四，大胜而归，排名从第5名上升至第3名",
      "canView": true,
      "challengerId": 1,
      "defenderId": 2
    },
    {
      "time": "01-10 14:25",
      "text": "王五 挑战 赵六，惜败，排名保持第8名",
      "canView": false,
      "challengerId": 3,
      "defenderId": 4
    }
  ]
}
```

**字段说明**：
- `time`: 挑战时间（格式：MM-DD HH:MM）
- `text`: 动态文本描述
- `canView`: 是否可以查看战报（只有与当前玩家相关的战斗才能查看）
- `challengerId`: 挑战者ID
- `defenderId`: 防守者ID

## ✅ 优点

1. **真实数据**：显示真实的挑战记录，不是写死的文字
2. **实时更新**：每次挑战后自动刷新动态
3. **赛区隔离**：只显示本赛区的动态
4. **时间排序**：按时间倒序显示，最新的在前
5. **可查看战报**：与自己相关的战斗可以查看详细战报（预留功能）

## 🔄 数据流程

```
玩家A挑战玩家B
  ↓
后端: 执行战斗
  ↓
后端: 更新排名
  ↓
后端: 保存挑战记录到 king_challenge_logs 表
  ↓
前端: 挑战成功后调用 loadDynamics()
  ↓
前端: GET /api/king/dynamics
  ↓
后端: 查询该赛区最近20条记录
  ↓
后端: 格式化动态文本
  ↓
后端: 返回动态列表
  ↓
前端: 显示在页面上
```

## 📝 相关文件

### 数据库
- `sql/014_king_challenge_logs.sql` - 挑战记录表（新建）

### 后端
- `interfaces/routes/king_routes.py` - 召唤之王路由（已修改）
  - 挑战接口：保存记录
  - 新增接口：`/dynamics` 获取动态

### 前端
- `interfaces/client/src/features/king/SummonKingChallengePage.vue` - 挑战页面（已修改）
  - 添加 `loadDynamics()` 函数
  - 修改模板显示真实数据

### 部署
- `dev_helper/部署召唤之王动态功能.bat` - 部署脚本

## 🚀 部署步骤

1. **执行SQL脚本**
   ```bash
   mysql -u root -p game_dev < sql/014_king_challenge_logs.sql
   ```
   或者运行：`dev_helper/部署召唤之王动态功能.bat`

2. **重启后端服务**
   - 停止当前服务（Ctrl+C）
   - 重新运行：`python interfaces/web_api/app.py`

3. **刷新前端页面**
   - 按 F5 或 Ctrl+F5 强制刷新

4. **测试功能**
   - 进入召唤之王挑战赛
   - 进行几次挑战
   - 查看动态是否显示真实记录

## 🐛 常见问题

### Q: 动态显示"暂无动态"
**A**: 
1. 确保数据库表已创建
2. 进行几次挑战产生记录
3. 检查后端日志是否有错误

### Q: 动态不更新
**A**: 
1. 检查挑战后是否调用了 `loadDynamics()`
2. 检查浏览器控制台是否有错误
3. 检查后端是否正确保存记录

### Q: 动态文本格式不对
**A**: 
1. 检查数据库中的记录是否正确
2. 检查后端格式化逻辑
3. 查看后端日志

## 🎉 总结

召唤之王挑战赛的动态功能现在会显示真实的挑战记录，包括：
- 挑战者和防守者
- 挑战结果（胜利/失败）
- 排名变化
- 挑战时间

这样玩家可以看到赛区内的实时动态，增加游戏的互动性和竞争氛围。
