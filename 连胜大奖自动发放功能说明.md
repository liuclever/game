# 连胜大奖自动发放功能说明

## 功能需求

连胜竞技场的连胜大奖需要重新设置领取逻辑：
- **当天全服连胜次数最高者**，直接在**23点由系统将奖励发在背包里**
- 在连胜竞技场界面说明下连胜大奖的领取方式

## 修改内容

### 1. 后端定时任务

**文件**：`infrastructure/scheduler.py`

**新增函数**：`_run_arena_streak_grand_prize()`

**功能**：每天23:00自动执行，为当天的全服连胜王发放大奖

**逻辑**：
1. 查询今天的全服连胜王（`max_streak_today` 最高的玩家）
2. 检查连胜次数是否大于0
3. 检查是否已发放过
4. 发放奖励到背包：
   - 铜钱×600,000
   - 追魂法宝×1 (item_id: 6019)
   - 金袋×5 (item_id: 6005)
   - 招财神符×1 (item_id: 6004)
5. 标记已发放（`claimed_grand_prize = 1`）

**注册定时任务**：
```python
_scheduler.add_job(
    _run_arena_streak_grand_prize,
    trigger="cron",
    hour=23,
    minute=0,
    id="arena_streak_grand_prize",
    replace_existing=True,
)
```

### 2. 前端页面修改

**文件**：`interfaces/client/src/views/ArenaStreak.vue`

**修改内容**：
- 移除了"领取"按钮和相关的领取逻辑
- 更新说明文字为："当天全服连胜次数最高者，系统将在23:00自动发放到背包"

**修改前**：
```vue
连胜大奖（昨日连胜王专属，领取时间00:00-08:00）: 铜钱60万+追魂法宝1+金袋5+招财神符1. 
<a class="link" @click="claimGrandPrize" v-if="canClaimGrandPrize">领取</a>
```

**修改后**：
```vue
连胜大奖（当天全服连胜次数最高者，系统将在23:00自动发放到背包）: 铜钱60万+追魂法宝1+金袋5+招财神符1
```

## 工作流程

### 时间线

1. **8:00-23:00**：连胜竞技场开放，玩家可以进行战斗
2. **23:00**：
   - 连胜竞技场关闭
   - 定时任务自动执行
   - 查询当天的全服连胜王
   - 自动发放大奖到背包
3. **次日 0:00**：
   - 连胜记录重置
   - 新的一天开始

### 发放规则

1. **资格判定**：
   - 当天 `max_streak_today` 最高的玩家
   - 如果有多个玩家连胜次数相同，取第一个（`ORDER BY max_streak_today DESC LIMIT 1`）

2. **连胜要求**：
   - 连胜次数必须大于0
   - 如果当天没有人连胜，不发放大奖

3. **防重复发放**：
   - 检查 `claimed_grand_prize` 字段
   - 如果已发放，跳过

4. **自动发放**：
   - 无需玩家手动领取
   - 奖励直接发放到背包
   - 铜钱直接增加到账户

## 奖励内容

| 奖励 | 数量 | 物品ID | 说明 |
|------|------|--------|------|
| 铜钱 | 600,000 | - | 直接增加到账户 |
| 追魂法宝 | 1 | 6019 | 发放到背包 |
| 金袋 | 5 | 6005 | 发放到背包 |
| 招财神符 | 1 | 6004 | 发放到背包 |

## 日志记录

定时任务会记录以下日志：

```
[Scheduler] 开始执行连胜大奖自动发放任务
[Scheduler] 连胜大奖发放成功: user_id=8, streak=10
```

如果没有连胜记录或已发放，会记录相应的跳过信息。

## 测试步骤

### 1. 重启后端服务

修改代码后，必须重启后端服务：

```bash
restart_flask.bat
```

### 2. 模拟测试（可选）

如果需要立即测试，可以临时修改定时任务的触发时间：

```python
# 临时修改为每分钟执行一次
_scheduler.add_job(
    _run_arena_streak_grand_prize,
    trigger="interval",
    minutes=1,
    id="arena_streak_grand_prize",
    replace_existing=True,
)
```

### 3. 正常测试流程

1. **准备测试数据**：
   - 使用测试账号进行连胜竞技场战斗
   - 确保有玩家达到连胜

2. **等待23:00**：
   - 等待到23:00定时任务执行
   - 或者手动触发定时任务（开发环境）

3. **检查结果**：
   - 查看后端日志，确认任务执行
   - 登录连胜王账号，检查背包
   - 确认铜钱和道具是否到账

4. **验证数据库**：
   ```sql
   SELECT user_id, max_streak_today, claimed_grand_prize 
   FROM arena_streak 
   WHERE record_date = CURDATE()
   ORDER BY max_streak_today DESC;
   ```

## 注意事项

### 1. 时区问题

定时任务使用中国时区（UTC+8），确保服务器时间正确。

### 2. 并发问题

定时任务使用 `claimed_grand_prize` 字段防止重复发放，即使任务执行多次也不会重复发放。

### 3. 背包容量

发放前不检查背包容量，如果背包满了，道具会自动扩展背包或放入临时背包。

### 4. 连胜次数相同

如果多个玩家连胜次数相同，只有第一个（按查询顺序）会获得大奖。

### 5. 旧的领取接口

旧的手动领取接口 `/api/arena-streak/claim-grand-prize` 仍然保留，但前端已移除领取按钮，玩家无法手动领取。

## 与旧逻辑的区别

| 项目 | 旧逻辑 | 新逻辑 |
|------|--------|--------|
| 领取方式 | 玩家手动领取 | 系统自动发放 |
| 领取时间 | 次日 00:00-08:00 | 当天 23:00 |
| 领取资格 | 昨日连胜王 | 当天连胜王 |
| 过期机制 | 超过08:00过期 | 无过期，自动发放 |
| 前端UI | 有"领取"按钮 | 无按钮，仅说明 |

## 相关文件

### 后端文件
- `infrastructure/scheduler.py` - 定时任务（已修改）
- `interfaces/routes/arena_streak_routes.py` - 连胜竞技场路由（保留旧接口）

### 前端文件
- `interfaces/client/src/views/ArenaStreak.vue` - 连胜竞技场页面（已修改）

### 数据库表
- `arena_streak` - 连胜记录表
  - `max_streak_today` - 今日最高连胜
  - `claimed_grand_prize` - 是否已领取大奖

## 总结

连胜大奖现在改为系统自动发放，玩家无需手动领取。每天23:00，系统会自动查询当天的全服连胜王，并将奖励直接发放到背包中。这样简化了操作流程，避免了玩家忘记领取或错过领取时间的问题。
